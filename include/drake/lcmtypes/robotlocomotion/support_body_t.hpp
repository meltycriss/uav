/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __robotlocomotion_support_body_t_hpp__
#define __robotlocomotion_support_body_t_hpp__

#include <vector>

namespace robotlocomotion
{

class support_body_t
{
    public:
        int64_t    utime;

        int32_t    body_id;

        int8_t     use_support_surface;

        int8_t     override_contact_pts;

        int32_t    num_contact_pts;

        std::vector< std::vector< double > > contact_pts;

        double     support_surface[4];

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "support_body_t"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int support_body_t::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = (int64_t)getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int support_body_t::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int support_body_t::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t support_body_t::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* support_body_t::getTypeName()
{
    return "support_body_t";
}

int support_body_t::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &this->utime, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->body_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->use_support_surface, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_encode_array(buf, offset + pos, maxlen - pos, &this->override_contact_pts, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &this->num_contact_pts, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    if(this->num_contact_pts > 0) {
        for (int a0 = 0; a0 < 3; a0++) {
            tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->contact_pts[a0][0], this->num_contact_pts);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    tlen = __double_encode_array(buf, offset + pos, maxlen - pos, &this->support_surface[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int support_body_t::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    tlen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this->utime, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->body_id, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->use_support_surface, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __boolean_decode_array(buf, offset + pos, maxlen - pos, &this->override_contact_pts, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &this->num_contact_pts, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    this->contact_pts.resize(3);
    for (int a0 = 0; a0 < 3; a0++) {
        if(this->num_contact_pts) {
            this->contact_pts[a0].resize(this->num_contact_pts);
            tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->contact_pts[a0][0], this->num_contact_pts);
            if(tlen < 0) return tlen; else pos += tlen;
        }
    }

    tlen = __double_decode_array(buf, offset + pos, maxlen - pos, &this->support_surface[0], 4);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int support_body_t::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += __int64_t_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += __boolean_encoded_array_size(NULL, 1);
    enc_size += __int32_t_encoded_array_size(NULL, 1);
    enc_size += 3 * __double_encoded_array_size(NULL, this->num_contact_pts);
    enc_size += __double_encoded_array_size(NULL, 4);
    return enc_size;
}

uint64_t support_body_t::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0x728fbe08984041a7LL;
    return (hash<<1) + ((hash>>63)&1);
}

}

#endif
