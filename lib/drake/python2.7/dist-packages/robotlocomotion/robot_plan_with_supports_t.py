"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import robotlocomotion.support_sequence_t

import robotlocomotion.robot_plan_t

class robot_plan_with_supports_t(object):
    __slots__ = ["utime", "plan", "support_sequence", "is_quasistatic"]

    def __init__(self):
        self.utime = 0
        self.plan = robotlocomotion.robot_plan_t()
        self.support_sequence = robotlocomotion.support_sequence_t()
        self.is_quasistatic = False

    def encode(self):
        buf = BytesIO()
        buf.write(robot_plan_with_supports_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.utime))
        assert self.plan._get_packed_fingerprint() == robotlocomotion.robot_plan_t._get_packed_fingerprint()
        self.plan._encode_one(buf)
        assert self.support_sequence._get_packed_fingerprint() == robotlocomotion.support_sequence_t._get_packed_fingerprint()
        self.support_sequence._encode_one(buf)
        buf.write(struct.pack(">b", self.is_quasistatic))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != robot_plan_with_supports_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return robot_plan_with_supports_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = robot_plan_with_supports_t()
        self.utime = struct.unpack(">q", buf.read(8))[0]
        self.plan = robotlocomotion.robot_plan_t._decode_one(buf)
        self.support_sequence = robotlocomotion.support_sequence_t._decode_one(buf)
        self.is_quasistatic = bool(struct.unpack('b', buf.read(1))[0])
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if robot_plan_with_supports_t in parents: return 0
        newparents = parents + [robot_plan_with_supports_t]
        tmphash = (0xa0a81c58290536e2+ robotlocomotion.robot_plan_t._get_hash_recursive(newparents)+ robotlocomotion.support_sequence_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if robot_plan_with_supports_t._packed_fingerprint is None:
            robot_plan_with_supports_t._packed_fingerprint = struct.pack(">Q", robot_plan_with_supports_t._get_hash_recursive([]))
        return robot_plan_with_supports_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

